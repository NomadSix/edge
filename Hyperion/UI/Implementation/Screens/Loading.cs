using System;
using Edge.Hyperion.UI.Components;
using Microsoft.Xna.Framework;

namespace Edge.Hyperion.UI.Implementation.Screens {
	public class Loading:Screen {
		//Screen displayed while all clients connect. Loads assets (logic bits too?) from server into client memory
		public Loading(Game game) : base(game) {
		}
	}
}

